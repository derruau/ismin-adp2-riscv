# Informations

Ce dossier contient le code TD 3.

- Dans le dossier `hdl_src/cache_associatif` vous trouverez la version du code qui contient le cache associatif
- Dans le dossier `hdl_src/cache_direct` vous trouverez la version du code qui contient le cache direct


# Compilation du projet

Pour compiler le projet, vous pouvez utiliser, dans le dossier racine:
```bash
./build.sh
```

Cela compilera la dernière version du projet.
